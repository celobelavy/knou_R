x <- c(7, 2, 4, 9, 8, 6, 1, 5, 10, 3)
sort(x)    # 숫자를 오름차순으로 정렬
sum(x)     # 합
mean(x)    # 표본 평균
sum(x)/length(x)    # 표본 평균
var(x)     # 표본 분산
min(x)     # 최솟값
which.min(x)    # 최솟값이 있는 위치
summary(x)    # 기초 통계량
